import { CyrillicDirective } from './cyrillic.directive';

describe('CyrillicDirective', () => {
  it('should create an instance', () => {
    const directive = new CyrillicDirective();
    expect(directive).toBeTruthy();
  });
});
